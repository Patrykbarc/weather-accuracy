# Path to the SQLite database.
# Note the four slashes for an absolute path: sqlite:////absolute/path/weather.db
# Required - there is no default, so a missing value fails at startup.
DATABASE_URL=sqlite:///weather.db

# Log every SQL statement (useful when debugging queries).
DB_ECHO=false
